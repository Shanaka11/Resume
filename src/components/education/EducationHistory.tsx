import History from '../history/History';
import { content } from '../../content'

const EducationHistory = () => {
  return <History history={content.education}/>;
};

export default EducationHistory;
